on:
  release:
    types: [released]

jobs:
  version_bump:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: ✅ Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🌏 Set up Git
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

      - name: Extract tag name and clean version
        id: extract_version
        run: |
          tag_name="${GITHUB_REF##*/}"
          clean_version=$(echo "$tag_name" | tr -d '.')
          echo "tag_name=$tag_name" >> $GITHUB_ENV
          echo "clean_version=$clean_version" >> $GITHUB_ENV

      - name: Update version in variables.gradle
        run: |
          sed -i "s/versionCodeVersion = [0-9]\+/versionCodeVersion = ${{ env.clean_version }}/g" android/variables.gradle
          sed -i "s/versionNameVersion = '.*'/versionNameVersion = '${{ env.tag_name }}'/g" android/variables.gradle

      - name: ☁️ Commit and push changes
        uses: EndBug/add-and-commit@v9
        with:
          default_author: github_actions
          message: 'Bump version to ${{ env.tag_name }}'
